apiVersion: v1
kind: Service
metadata:
  name: caderno
  namespace: caderno
  labels:
    app.kubernetes.io/name: caderno
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: caderno
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
